<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="TagItem">
        <!-- layout -->
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:paddingHorizontal">14dp</item>
        <item name="android:layout_height">32dp</item>

        <!-- text -->
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>

        <!-- color -->
        <item name="android:textColor">@color/tag_text_selector</item>

        <!-- background -->
        <item name="android:background">@drawable/bg_tag_selector</item>

        <!-- interaction -->
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="TagRedItem">
        <!-- layout -->
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:paddingHorizontal">14dp</item>
        <item name="android:layout_height">32dp</item>

        <!-- text -->
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>

        <!-- color -->
        <item name="android:textColor">@color/tag_text_red_selector</item>

        <!-- background -->
        <item name="android:background">@drawable/bg_tag_red_selector</item>

        <!-- interaction -->
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="TagYellowItem">
        <!-- layout -->
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:paddingHorizontal">14dp</item>
        <item name="android:layout_height">32dp</item>

        <!-- text -->
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>

        <!-- color -->
        <item name="android:textColor">@color/tag_text_yellow_selector</item>

        <!-- background -->
        <item name="android:background">@drawable/bg_tag_yellow_selector</item>

        <!-- interaction -->
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="TagGreenItem">
        <!-- layout -->
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:paddingHorizontal">14dp</item>
        <item name="android:layout_height">32dp</item>

        <!-- text -->
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>

        <!-- color -->
        <item name="android:textColor">@color/tag_text_green_selector</item>

        <!-- background -->
        <item name="android:background">@drawable/bg_tag_green_selector</item>

        <!-- interaction -->
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>
</resources>
