<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClothShop">
        <activity
            android:name=".activity.user.CartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.user.CheckoutActivity" />
        <activity android:name=".activity.user.AddressListActivity" />
        <activity android:name=".activity.user.AddAddressActivity" />
        <activity android:name=".activity.user.PaymentMethodActivity" />
        <activity android:name=".activity.user.OrderSuccessActivity" />
        <activity android:name=".activity.user.OrderHistoryActivity" />
        <activity android:name=".activity.user.OrderDetailActivity" />
        <activity android:name=".activity.user.LeaveReviewActivity" />
        <activity android:name=".activity.user.ViewReviewActivity" />
    </application>

</manifest>